'use strict';

const DEFAULT_CATEGORY = 'default';
const CATEGORIES = [
  'all',
  'default',
  'options',
  'idl',
  'normalize',
  'graphql',
  'middleware',
  'server',
];

module.exports = {
  DEFAULT_CATEGORY,
  CATEGORIES,
};
