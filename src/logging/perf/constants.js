'use strict';

const DEFAULT_CATEGORY = 'default';
const CATEGORIES = [
  'all',
  'default',
  'options',
  'idl',
  'normalize',
  'validate',
  'graphql',
  'middleware',
  'server',
];

module.exports = {
  DEFAULT_CATEGORY,
  CATEGORIES,
};
