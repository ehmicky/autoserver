'use strict';

const DEFAULT_CATEGORY = 'default';
// Whitelist categories to avoid typos
const CATEGORIES = [
  'default',
  'main',
  'run_opts',
  'server',
  'middleware',
  'time',
  'protocol',
  'operation',
  'action',
  'command',
  'database',
  'final',
];

module.exports = {
  DEFAULT_CATEGORY,
  CATEGORIES,
};
