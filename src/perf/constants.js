'use strict';

const DEFAULT_CATEGORY = 'default';
// Whitelist categories to avoid typos
const CATEGORIES = [
  'all',
  'default',
  'main',
  'run_opts',
  'middleware',
  'server',
];

module.exports = {
  DEFAULT_CATEGORY,
  CATEGORIES,
};
