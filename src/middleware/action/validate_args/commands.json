{
  "findOne": {
    "optional": ["select", "silent", "params"],
    "required": ["id"]
  },
  "findMany": {
    "optional": [
      "select",
      "filter",
      "order",
      "pagesize",
      "page",
      "before",
      "after",
      "silent",
      "params"
    ],
    "required": []
  },
  "deleteOne": {
    "optional": ["select", "cascade", "silent", "dryrun", "params"],
    "required": ["id"]
  },
  "deleteMany": {
    "optional": [
      "select",
      "cascade",
      "filter",
      "pagesize",
      "silent",
      "dryrun",
      "params"
    ],
    "required": []
  },
  "patchOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["id", "single_data"]
  },
  "patchMany": {
    "optional": [
      "select",
      "filter",
      "pagesize",
      "silent",
      "dryrun",
      "params"
    ],
    "required": ["single_data"]
  },
  "createOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "createMany": {
    "optional": ["select", "pagesize", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  },
  "upsertOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "upsertMany": {
    "optional": ["select", "pagesize", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  }
}
