{
  "findOne": {
    "optional": [
      "populate",
      "select",
      "rename",
      "silent",
      "params"
    ],
    "required": [
      "id"
    ]
  },
  "findMany": {
    "optional": [
      "populate",
      "filter",
      "order",
      "pagesize",
      "page",
      "before",
      "after",
      "select",
      "rename",
      "silent",
      "params"
    ],
    "required": [
    ]
  },
  "deleteOne": {
    "optional": [
      "cascade",
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "id"
    ]
  },
  "deleteMany": {
    "optional": [
      "cascade",
      "filter",
      "pagesize",
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
    ]
  },
  "patchOne": {
    "optional": [
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "id",
      "single_data"
    ]
  },
  "patchMany": {
    "optional": [
      "filter",
      "pagesize",
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "createOne": {
    "optional": [
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "createMany": {
    "optional": [
      "pagesize",
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "multiple_data"
    ]
  },
  "upsertOne": {
    "optional": [
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "upsertMany": {
    "optional": [
      "pagesize",
      "select",
      "rename",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "multiple_data"
    ]
  }
}
