{
  "findOne": {
    "optional": [
      "populate",
      "select",
      "format",
      "silent",
      "params"
    ],
    "required": [
      "id"
    ]
  },
  "findMany": {
    "optional": [
      "populate",
      "filter",
      "order",
      "pagesize",
      "page",
      "before",
      "after",
      "select",
      "format",
      "silent",
      "params"
    ],
    "required": [
    ]
  },
  "deleteOne": {
    "optional": [
      "cascade",
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "id"
    ]
  },
  "deleteMany": {
    "optional": [
      "cascade",
      "filter",
      "pagesize",
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
    ]
  },
  "patchOne": {
    "optional": [
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "id",
      "single_data"
    ]
  },
  "patchMany": {
    "optional": [
      "filter",
      "pagesize",
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "createOne": {
    "optional": [
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "createMany": {
    "optional": [
      "pagesize",
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "multiple_data"
    ]
  },
  "upsertOne": {
    "optional": [
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "single_data"
    ]
  },
  "upsertMany": {
    "optional": [
      "pagesize",
      "select",
      "format",
      "silent",
      "dryrun",
      "params"
    ],
    "required": [
      "multiple_data"
    ]
  }
}
