{
  "findOne": {
    "optional": ["populate", "select", "silent", "params"],
    "required": ["id"]
  },
  "findMany": {
    "optional": [
      "populate",
      "filter",
      "order",
      "pagesize",
      "page",
      "before",
      "after",
      "select",
      "silent",
      "params"
    ],
    "required": []
  },
  "deleteOne": {
    "optional": ["cascade", "select", "silent", "dryrun", "params"],
    "required": ["id"]
  },
  "deleteMany": {
    "optional": [
      "cascade",
      "filter",
      "pagesize",
      "select",
      "silent",
      "dryrun",
      "params"
    ],
    "required": []
  },
  "patchOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["id", "single_data"]
  },
  "patchMany": {
    "optional": [
      "filter",
      "pagesize",
      "select",
      "silent",
      "dryrun",
      "params"
    ],
    "required": ["single_data"]
  },
  "createOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "createMany": {
    "optional": ["pagesize", "select", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  },
  "upsertOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "upsertMany": {
    "optional": ["pagesize", "select", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  }
}
