'use strict';

// Input attributes passed along the whole request, i.e. common to all layers
const commonAttributes = [
  'idl',
  'runOpts',
  'ifv',
  'currentPerf',
  'perf',
  'reqInfo',
];

module.exports = {
  commonAttributes,
};
