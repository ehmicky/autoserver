{
  "findOne": {
    "optional": ["select", "silent", "params"],
    "required": ["single_filter"]
  },
  "findMany": {
    "optional": [
      "select",
      "multiple_filter",
      "order_by",
      "page_size",
      "page",
      "before",
      "after",
      "silent",
      "params"
    ],
    "required": []
  },
  "deleteOne": {
    "optional": ["select", "cascade", "silent", "dryrun", "params"],
    "required": ["single_filter"]
  },
  "deleteMany": {
    "optional": [
      "select",
      "cascade",
      "multiple_filter",
      "page_size",
      "silent",
      "dryrun",
      "params"
    ],
    "required": []
  },
  "updateOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_filter", "single_data"]
  },
  "updateMany": {
    "optional": [
      "select",
      "multiple_filter",
      "page_size",
      "silent",
      "dryrun",
      "params"
    ],
    "required": ["single_data"]
  },
  "createOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "createMany": {
    "optional": ["select", "page_size", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  },
  "replaceOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "replaceMany": {
    "optional": ["select", "page_size", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  },
  "upsertOne": {
    "optional": ["select", "silent", "dryrun", "params"],
    "required": ["single_data"]
  },
  "upsertMany": {
    "optional": ["select", "page_size", "silent", "dryrun", "params"],
    "required": ["multiple_data"]
  }
}
