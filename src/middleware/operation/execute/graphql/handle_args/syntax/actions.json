{
  "findOne": {
    "optional": ["select"],
    "required": ["single_filter"]
  },
  "findMany": {
    "optional": [
      "select",
      "multiple_filter",
      "order_by",
      "page_size",
      "page",
      "before",
      "after"
    ],
    "required": []
  },
  "deleteOne": {
    "optional": ["select", "cascade"],
    "required": ["single_filter"]
  },
  "deleteMany": {
    "optional": [
      "select",
      "cascade",
      "multiple_filter",
      "order_by",
      "page_size"
    ],
    "required": []
  },
  "updateOne": {
    "optional": ["select"],
    "required": ["single_filter", "single_data"]
  },
  "updateMany": {
    "optional": ["select", "multiple_filter", "page_size"],
    "required": ["single_data"]
  },
  "createOne": {
    "optional": ["select"],
    "required": ["single_data"]
  },
  "createMany": {
    "optional": ["select", "page_size"],
    "required": ["multiple_data"]
  },
  "replaceOne": {
    "optional": ["select"],
    "required": ["single_data"]
  },
  "replaceMany": {
    "optional": ["select", "page_size"],
    "required": ["multiple_data"]
  },
  "upsertOne": {
    "optional": ["select"],
    "required": ["single_data"]
  },
  "upsertMany": {
    "optional": ["select", "page_size"],
    "required": ["multiple_data"]
  }
}
